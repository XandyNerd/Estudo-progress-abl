# Arquitetura de Sistemas ERP (Progress OpenEdge ABL)

Existem duas formas principais de como um ERP funciona no mundo Progress:

---

## 1. Modelo Clássico (Client-Server)
- **Onde roda a lógica?** No computador do cliente.
- **Papel do Servidor:** Apenas gerenciar o Banco de Dados.
- **Como funciona:** O aplicativo na máquina do usuário conecta diretamente ao banco via rede. 
- **Desvantagem:** Se a rede for lenta, o sistema trava, pois trafega muitos dados brutos.

## 2. Modelo Moderno (Three-Tier / PASOE)
- **Onde roda a lógica?** No Servidor de Aplicação (PASOE).
- **Papel do Servidor:** É o "Cérebro". Toma as decisões, valida regras e acessa o banco.
- **Papel do Cliente:** É apenas o "Rosto". Desenha a tela e envia comandos para o servidor.
- **Vantagem:** Muito mais rápido e seguro. Ideal para rodar sistemas via Web ou Cloud.

---

## Comparação com Desenvolvimento Web:

| Conceito Web | Equivalente Progress ABL (Moderno) |
| :--- | :--- |
| **Frontend** (React/HTML) | **Interface GUI** (.w ou .p visual) |
| **Backend** (Node.js/Python) | **PASOE** (Onde o código ABL de servidor roda) |
| **SQL Database** | **Progress DB** (O banco .db) |
| **API / Endpoint** | **Chamada de Procedimento** (Ex: RUN arquivo.p ON SERVER) |

---
*Documento de estudo sobre arquitetura Progress OpenEdge.*

## 3. Organização de Código: Lógica vs. Interface

### Opção A: Lógica no arquivo .w (Monolítico)
*   **O que é:** O código de validação (ex: busca no banco) fica dentro do objeto da tela.
*   **Vantagem:** Desenvolvimento mais rápido; tudo em um só lugar.
*   **Desvantagem:** Prisão tecnológica. Se precisar do mesmo login em uma página Web, terá que duplicar o código.

### Opção B: Lógica no arquivo .p (Arquitetura em Camadas)
*   **O que é:** A interface (.w) apenas captura os dados e chama um procedimento (.p) externo para processar.
*   **Vantagem:** REUSO. O mesmo .p de lógica pode servir para GUI, Web, Mobile e Integrações.
*   **Conceito Profissional:** "Business Logic Layer" (Camada de Regras de Negócio).

---
*Exemplo de chamada de lógica externa:*
RUN valida_login.p (INPUT p_usuario, INPUT p_senha, OUTPUT l_sucesso).

## 4. Acesso Multi-Plataforma Simultâneo

É perfeitamente possível (e comum) ter usuários acessando o mesmo sistema por meios diferentes ao mesmo tempo:

*   **Usuário A:** Abre o executável `.bat` direto no Windows (Interface GUI clássica).
*   **Usuário B:** Abre o navegador Chrome e acessa o portal da empresa (Interface Web HTML).

### Como isso funciona?
Ambos os usuários batem na mesma **Camada de Lógica (.p)** no servidor. O servidor não se importa se quem perguntou foi um botão do Windows ou um botão do site; ele apenas processa a regra de negócio e devolve o resultado.

**Resultado:** Os dois usuários enxergam os mesmos dados no banco em tempo real, cada um na sua interface preferida. 🚀
